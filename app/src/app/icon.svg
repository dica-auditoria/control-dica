<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- White background -->
  <rect width="100" height="100" fill="white"/>

  <!-- "D" in dark blue -->
  <text x="3" y="84" font-family="Arial, Helvetica, sans-serif" font-weight="900" font-size="90" fill="#1B4F8A">D</text>

  <!-- "i" green stem -->
  <rect x="60" y="28" width="12" height="55" fill="#8DC63F" rx="2"/>

  <!-- "i" green dot (square, same as logo) -->
  <rect x="60" y="10" width="12" height="12" fill="#8DC63F" rx="1"/>
</svg>
